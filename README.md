# Artificial-Neural-Network-for-Regression-Classification
ANN model in PyTorch for regression &amp; classification. Used nn.Module, TensorDataset, DataLoader, Linear layers + ReLU. Loss: MSE (regression), Cross Entropy (classification). Metrics: MSE &amp; manual accuracy. Best model saved with torch.save.
